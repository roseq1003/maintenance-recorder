[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "maintenance-recorder-package"
version = "0.1.0"
description = "Auto-generated Python package structure"
authors = [
    { name = "Kota Kawano", email = "" }
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = []
